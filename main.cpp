#include <iostream>
#include <thread>
#include <vector>

using namespace std;

int main(int argc, char* argv[])
{
    cout << "Hello, World!" << endl;
    return 0;
}

void parallel_execute(vector<int> incoming_job, int num_threads, bool is_dependent)
{
  // Create threads and execute jobs in parallel

}
